[project]
name = "webstaffr"
version = "3.3.0"
description = "WebStaffr local service business platform"
requires-python = ">=3.13"

[project.optional-dependencies]
dev = ["pytest==8.4.2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
